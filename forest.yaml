repo: misty-step/canopy
agents:
  builder:  { poll: "./forest poll builder",  interval: 300 }
  verifier: { poll: "./forest poll verifier", interval: 120 }
  fixer:    { poll: "./forest poll fixer",    interval: 300 }
  critic:   { poll: "./agents/critic/poll.sh", interval: 86400 }
  tester:   { poll: "./agents/tester/poll.sh", interval: 86400 }
checks:
  - name: test
    run: go test ./...
  - name: vet
    run: go vet ./...
